services:
  - type: web
    name: pions-invoice-app
    env: docker  # This forces Render to use your Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000
      # NO PASSWORDS HERE. We will add them in the UI.
